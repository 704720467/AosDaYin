2017年12月15日15:32:28
1、手机和打印机通过nfc连接有时会出现只是打印机打开、app打开但蓝牙未连接成功的情况；
    但是发现通过nfc接触会出现蓝牙无法自动连接的问题
    这个有两块代码，一块是在主界面中我添加的有一个初始化打印机的函数，一块是在回调中有一个onPrinterDiscovery()的函数
    当检测到nfc的时候，会调用这两个地方的一个
2、第一个标签在界面打完一遍后再按打印按钮弹出的提示文字里将“商品识别标签”改为“转运路由标签”；（解决）
3、app图标美化一下